#!/bin/sh

# This script is a convenience script to start the ipl registry server from the
# IPL installation, specified in the $IPL_HOME environment variable.


exec bin/ipl-run -Xmx256M ibis.ipl.server.Server "$@"
